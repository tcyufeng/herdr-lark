---
description: 关闭当前会话的飞书远程模式
---
1. 先看有没有还挂在手机上的提问（`herdr-lark status`，或你自己有在跑的后台 ask 任务）。
   有的话先停掉那个后台任务，飞书卡片会变成「⚠️ 已取消」——别让用户手机上留一张死卡。
2. 执行 `herdr-lark away off`，把 stdout 转告用户。

**只关当前会话。** 用户要是说「我回来了」这种意思，用 `/away-off-all` —— 人回来了是所有会话都回来了。

daemon 留着不用停。
