---
description: 开启所有会话的飞书远程模式（每个已绑定的会话都推手机）
---
执行 `herdr-lark away on --all`，把 stdout 原样转告用户。

这会把**所有已经绑定过飞书群的会话**都切到远程模式，不只是当前这个。
用在用户要离开较久、手上好几个会话都在跑的时候。

注意：它只对**已经绑定过**的会话生效，不会替没绑过的会话建群——那些需要各自跑一次 `/away-on`。
