---
description: 我回来了 —— 关闭所有会话的飞书远程模式
---
用户回到键盘前了。人只有一个，回来就是所有会话都回来了。

1. 先看有没有还挂在手机上的提问（`herdr-lark status` 里「挂起提问」不为 0）。
   有的话先停掉对应的后台 ask 任务，卡片会变成「⚠️ 已取消」，别留死卡在他手机上。
2. 执行 `herdr-lark away off --all`，把 stdout 转告用户（它会报关掉了几个会话）。

daemon 留着不用停。之后照常在终端交互。
